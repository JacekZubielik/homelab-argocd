apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

metadata:
  labels:
    app.kubernetes.io/name: longhorn
  annotations:
    argocd.argoproj.io/sync-options: SkipDryRunOnMissingResource=true
    argocd.argoproj.io/compare-options: IgnoreExtraneous

helmCharts:
- name: longhorn
  includeCRDs: true
  releaseName: longhorn
  version: v1.5.3
  repo: https://charts.longhorn.io
  valuesFile: longhorn-values.yaml #values.yaml

  # valuesInline:
  #   installCRDs: true
  #   namespace: longhorn-system
  #   fullnameOverride: longhorn-system

configMapGenerator:
- name: longhorn-config
  literals:
  - TZ=CET #"Europe/Warsaw"

# resources:
#   - resources/ingressroute.yaml
#- resources/ingress.yaml
  # - resources/storageclass.yaml

# patchesStrategicMerge:
#   - resources/storageclass.yaml

# patches:
#   - path: patches/ignore-deployment-mutation-patch.yaml
#     target:
#       kind: Deployment

  # - path: resources/storageclass.yaml
  #   target:
  #     kind: StorageClass