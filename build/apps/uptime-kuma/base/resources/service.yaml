---
apiVersion: v1
kind: Service
metadata:
  name: uptime-kuma
  annotations:
    argocd.argoproj.io/sync-wave: "0"
  # labels:
  #   app.kubernetes.io/name: uptime-kuma
  #   app: uptime-kuma
  #   name: uptime-kuma
spec:
  ports:
    - port: 3001
      protocol: TCP
      targetPort: 80
  selector:
    app: uptime-kuma