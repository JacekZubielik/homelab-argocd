apiVersion: v1
kind: Service
metadata:
  name: whoami-frontend

spec:
  ports:
    - port: 80
      protocol: TCP
      name: http
  # selector:
  #   app: traefik
  #   task: whoami
  selector:
    #app: traefik
    app.kubernetes.io/name: traefik
    task: whoami-frontend