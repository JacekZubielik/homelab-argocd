apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
- deployment.yaml
- service.yaml
- ingress.yaml
#- ingressroute.yaml

commonLabels:
  app.kubernetes.io/name: whoami
  app: whoami

configMapGenerator:
- name: whoami-config
  literals:
  - TZ="Europe/Warsaw"

