---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: dashboard
  namespace: traefik
  labels:
    app.kubernetes.io/name: traefik
  annotations:
    kubernetes.io/ingress.class: traefik-external

    #cert-manager.io/cluster-issuer: route53-issuer-staging
    #cert-manager.io/cluster-issuer: route53-issuer-production
    # gethomepage.dev/description: traefik test tool
    # gethomepage.dev/enabled: "true"
    # gethomepage.dev/group: Tools
    # gethomepage.dev/icon: homepage.png
    # gethomepage.dev/name: traefik
    # gethomepage.dev/widget.type: "traefik"
    # gethomepage.dev/widget.url: "https://traefik.anotherlife.pl"

spec:
  entryPoints:
    - websecure
  routes:
    #- match: Host(`traefik.<secret:private-domain>`) && (PathPrefix(`/dashboard`) || PathPrefix(`/api`))
    - match: Host(`traefik.infra.anotherlife.pl`) && (PathPrefix(`/dashboard`) || PathPrefix(`/api`))
      kind: Rule
      services:
        - name: api@internal
          kind: TraefikService
      # middlewares:
      #   - name: traefik-dashboard-auth  #chain-traefik-dashboard
      #     namespace: traefik
  tls:
    secretName: letsencrypt-production
    # options:
    #   name: tlsoptions
    # domains:
    #   - main: anotherlife.pl
    #     sans:
    #       - .anotherlife.pl