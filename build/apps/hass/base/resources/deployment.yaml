---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: home-assistant
  labels:
    app.kubernetes.io/name: home-assistant
    app: home-assistant
    name: home-assistant

spec:
  revisionHistoryLimit: 3
  replicas: 1
  progressDeadlineSeconds: 600
  strategy:
    type: Recreate

  selector:
    matchLabels:
      app.kubernetes.io/name: home-assistant

  template:
    metadata:
      labels:
        app.kubernetes.io/name: home-assistant
        task: home-assistant
    spec:
      #hostNetwork: true
      containers:
      - name: home-assistant
        image: "ghcr.io/home-assistant/home-assistant:2024.2.0.dev20240129"
        imagePullPolicy: IfNotPresent
        # securityContext:
        #   privileged: null
        ports:
          - name: http
            containerPort: 8123
            protocol: TCP

        # livenessProbe:
        #   tcpSocket:
        #     port: 8123
        #   initialDelaySeconds: 0
        #   failureThreshold: 3
        #   timeoutSeconds: 1
        #   periodSeconds: 10
        # readinessProbe:
        #   tcpSocket:
        #     port: 8123
        #   initialDelaySeconds: 0
        #   failureThreshold: 3
        #   timeoutSeconds: 1
        #   periodSeconds: 10
        # startupProbe:
        #   tcpSocket:
        #     port: 8123
        #   initialDelaySeconds: 0
        #   failureThreshold: 30
        #   timeoutSeconds: 1
        #   periodSeconds: 5

        volumeMounts:
          - name: hass-data-volume
            mountPath: /config
      volumes:
        - name: hass-data-volume
          persistentVolumeClaim:
            claimName: pvc-hass-data


      #     env:
      #       - name: TZ
      #         valueFrom:
      #           configMapKeyRef:
      #             name: homeassistant-env
      #             key: TZ
      #     volumeMounts:
      #       - name: homeassistant-data-volume
      #         mountPath: /config
      # volumes:
      #   - name: homeassistant-data-volume
      #     persistentVolumeClaim:
      #       claimName: pvc-nfs-kubedata-homeassistant-data


      #     env:
      #       - name: TZ
      #         value: "${TIMEZONE}"
      #     volumeMounts:
      #       - mountPath: /config
      #         name: home-assistant-content
      # volumes:
      #   - name: home-assistant-content
      #     persistentVolumeClaim:
      #       claimName: home-assistant-claim




      



          # resources:
          #   requests:
          #     memory: "128Mi"
          #     cpu: "100m"
          #   limits:
          #     memory: "512Mi"
          #     cpu: "500m"





