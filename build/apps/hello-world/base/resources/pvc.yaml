---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: hello-world-frontend
  annotations:
    argocd.argoproj.io/sync-wave: "4"
  labels:
    app.kubernetes.io/name: hello-world-frontend

spec:
  storageClassName: local-path #longhorn-retain
  #volumeName: hello-world-frontend
  accessModes:
    - ReadWriteOnce #ReadWriteMany #ReadWriteOnce
  #volumeMode: Block
  resources:
    requests:
      storage: 2Gi
  # selector:
  #   matchLabels:
  #     volume: hello-world-frontend