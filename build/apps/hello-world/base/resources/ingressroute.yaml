---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: hello-world
  namespace: dev
  labels:
    app.kubernetes.io/name: hello-world

  annotations:
    cert-manager.io/cluster-issuer: route53-letsencrypt-staging
    kubernetes.io/ingress.class: traefik-external
    #argocd.argoproj.io/sync-options: SkipDryRunOnMissingResource=true
    #argocd.argoproj.io/sync-wave: "4"

spec:
  entryPoints:
    - secure
  routes:
    - kind: Rule
      match: "Host(`hello.dev.anotherlife.pl`)" #" && PathPrefix(`/`)"
      services:
        - kind: Service
          name: hello-world
          #namespace: dev
          port: 80
      # middlewares:
      #   - name: traefik-dashboard-auth  #chain-traefik-dashboard
      #     namespace: traefik
  tls:
    #certResolver: anotherlife-letsencrypt-staging
    secretName: hello.dev.letsencrypt-staging # -staging / -production
    # options:
    #   name: tls-ver13
    domains:
      - main: anotherlife.pl
        sans:
          - .anotherlife.pl
