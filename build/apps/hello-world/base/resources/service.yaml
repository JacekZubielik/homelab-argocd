apiVersion: v1
kind: Service
metadata:
  name: hello-world-frontend
  # labels:
  #   app: hello-world
spec:
  ports:
    - port: 80
      protocol: TCP
      targetPort: 8080
      name: http
  selector:
    app: traefik
    task: hello-world