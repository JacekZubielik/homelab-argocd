---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: hello-world
  namespace: dev
  labels:
    app.kubernetes.io/name: traefik
  annotations:
    kubernetes.io/ingress.class: traefik-internal
spec:
  entryPoints:
    - websecure
  routes:
    - kind: Rule
      match: "Host(`ha.anotherlife.pl`)" #&&  PathPrefix(`/dashboard`) || PathPrefix(`/api`)"
      services:
        - kind: Service
          name: hello-world
          namespace: dev
          port: 80
      # middlewares:
      #   - name: traefik-dashboard-auth  #chain-traefik-dashboard
      #     namespace: traefik
  tls:
    store:
      name: store-anotherlife-pl-tls-staging
    # options:
    #   name: tlsoptions
    #   namespace: traefik
    domains:
      - main: anotherlife.pl
        sans:
          - .anotherlife.pl
