apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
- resources/deployment.yaml
#- resources/ingress.yaml
- resources/ingressroute.yaml
- resources/service.yaml
#- resources/servicemonitor.yaml
#- resources/sealed-sec.yaml
#- resources/secret-test.yaml
#- resources/pvc.yaml

commonLabels:
  app.kubernetes.io/name: hello-world-frontend
  app: hello-world-frontend

configMapGenerator:
- name: hello-world-frontend-config
  literals:
  - TZ="Europe/Warsaw"

  # valuesInline:
  #   alertmanager:
  #     persistentVolume:
  #       storageClass: manual
  #   server:
  #     persistentVolume:
  #       storageClass: manual
