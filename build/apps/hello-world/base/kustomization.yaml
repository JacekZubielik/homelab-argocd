apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
- deployment.yaml
- service.yaml
- ingress.yaml
#- ingressroute.yaml

commonLabels:
  app.kubernetes.io/name: hello-world
  app: hello-world

configMapGenerator:
- name: hello-world-config
  literals:
  - TZ="Europe/Warsaw"

